function setup() {
    new Canvas(800, 600);
    background(150); // grayscale 0 - 255 black - white

    noStroke();
    circle(100, 100, 50);
    
    fill(255, 0, 0);
    stroke(255, 255 ,0); // (r, g, b)
    strokeWeight(10);
    rect(200, 200, 50, 50);

    for (let i = 0; i < 10; i++) {
        console.log
    }
}

function draw() {
    
}